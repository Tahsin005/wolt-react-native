import { Text, View } from 'react-native';

export default function Page() {
    return (
        <View style={{
            // backgroundColor: "#000"
        }}>
            <Text>Discovery Page</Text>
        </View>
    );
}